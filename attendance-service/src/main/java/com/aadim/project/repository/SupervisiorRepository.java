package com.aadim.project.repository;

import com.aadim.project.entity.Supervisior;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SupervisiorRepository extends JpaRepository<Supervisior, Integer> {
}
